name: End-to-End

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  E2E_AUTH_ENABLED: false

on:
  pull_request:
    branches:
      - main
      - "release/**"
  workflow_dispatch:

concurrency:
  group: e2e-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  smoke:
    uses: ./.github/workflows/reusable-integration-smoke.yml
